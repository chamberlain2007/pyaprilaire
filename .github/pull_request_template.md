## Summary

<!-- What does this PR do, and why? -->

## Type of change

<!-- Delete the ones that don't apply -->

- feat: New feature
- fix: Bug fix
- docs: Documentation only
- refactor: Code change that neither fixes a bug nor adds a feature
- test: Adding or correcting tests
- chore: Tooling, CI, or other maintenance

## Breaking changes

<!--
If this PR changes existing behaviour in a way that could break downstream code,
put a `!` after the type/scope in the PR TITLE (e.g. `feat(client)!: ...`) and
describe the break below, starting with `BREAKING CHANGE:`.

The PR title is what release notes are built from, so a `!` on an inner commit
alone will not appear there. See CONTRIBUTING.md.

Otherwise write "None".
-->

None

## Testing

<!-- How was this verified? e.g. `pytest`, manual testing against a thermostat/mock server -->

## Checklist

- [ ] PR title follows [Conventional Commits](https://www.conventionalcommits.org/) (e.g. `feat: add support for X`)
- [ ] If this is a breaking change, the PR title carries `!` after the type/scope
- [ ] Commits follow Conventional Commits (see [CONTRIBUTING.md](../CONTRIBUTING.md))
- [ ] Tests added/updated as needed
- [ ] `ruff check` and `ruff format --check` pass
